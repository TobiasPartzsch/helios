{
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "test": "vitest",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "@types/node": "^25.5.0",
        "gh-pages": "^6.3.0",
        "vite": "^8.0.1",
        "vitest": "^4.1.0"
    }
}