{
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "test": "vitest",
        "deploy": "npm run build && gh-pages -d dist",
        "format": "prettier --write src/",
        "generate:vsop87": "tsx scripts/parseVsop87.ts",
        "generate:horizons": "tsx scripts/fetch-horizons-reference.ts",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/node": "^25.5.0",
        "gh-pages": "^6.3.0",
        "prettier": "^3.8.1",
        "ts-node": "^10.9.2",
        "tsx": "^4.21.0",
        "vite": "^8.0.1",
        "vitest": "^4.1.0"
    }
}